version: '3.7'

services:

  loghell:
    container_name: loghell
    build: .
    image: loghell
    ports:
      - $HTTP_PORT:3033
      - $TCP_PORT:3031
      - $WS_PORT:3032
    env_file:
      - .env
    networks:
      - default

networks:
  default:
    name: loghell
